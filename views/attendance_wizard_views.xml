<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_school_attendance_wizard_form" model="ir.ui.view">
            <field name="name">school.attendance.wizard.form</field>
            <field name="model">school.attendance.wizard</field>
            <field name="arch" type="xml">
                <form string="Register Class Attendance">
                    <sheet>
                        <group>
                            <field name="class_id"/>
                            <field name="date"/>
                        </group>

                        <separator string="Students in Class"/>
                        <field name="student_ids" nolabel="1" readonly="1">
                            <tree>
                                <field name="roll_number"/>
                                <field name="name"/>
                            </tree>
                        </field>
                        </sheet>
                    <footer>
                        <button name="action_create_attendance" string="Create &amp; Mark Present" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_school_attendance_wizard" model="ir.actions.act_window">
            <field name="name">Register Attendance</field>
            <field name="res_model">school.attendance.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field> </record>

    </data>
</odoo>