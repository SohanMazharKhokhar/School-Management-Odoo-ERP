<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_fee_generation_wizard_form" model="ir.ui.view">
            <field name="name">fee.generation.wizard.form</field>
            <field name="model">fee.generation.wizard</field>
            <field name="arch" type="xml">
                <form string="Generate Fees">
                    <group>
                        <group>
                            <field name="class_id" options="{'no_create': True, 'no_open': True}"/>
                            <field name="fee_type_id" options="{'no_create': True, 'no_open': True}"/>
                        </group>
                        <group>
                            <field name="due_date"/>
                        </group>
                    </group>
                    <footer>
                        <button name="action_generate_fees" string="Generate Fees" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_fee_generation_wizard" model="ir.actions.act_window">
            <field name="name">Generate Class Fees</field>
            <field name="res_model">fee.generation.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field> </record>

    </data>
</odoo>